/**
 * USAGE
 * import UTILS_CHORDIDENTIFIER from '...';
 * 
 * data() {
    return {
      chordIdentifier: UTILS_CHORDIDENTIFIER,
    };
  },
 */

/**
 * TODO
 * create a method that receive :
 * - an array of notes (e.g. [0, 4, 7])
 * - a tonality (e.g. "C", "F#")
 * 
 * Respond with a string that identifie it
 * (e.g. : "C", "CMaj7")
 */

 export default {
     chordIdentifier(notes, tonality) {
        var identifiedChord = '';
        return identifiedChord;
     }
 };
